pipeline {
  agent none
  stages {
    stage('Back-end') {
      agent {
        docker { image 'mcr.microsoft.com/dotnet/framework/samples:dotnetapp' }
      }
      steps {
        sh 'sdk --version'
      }
    }    
  }
}
